# it is assumed that by this point you would have xcode cli and git installed
# xcode cli can be installed using the following command
# xcode-select --install
# Or, simply try to git clone this repo
# Your Mac will prompt you to install xcode cli

# install Homebrew
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install.sh)"

